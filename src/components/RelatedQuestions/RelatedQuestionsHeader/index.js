import React from "react";
import { StyledHeader } from "./styled";

const relatedQuestionsTitle = "People also ask";

const RelatedQuestionsHeader = () => {
  return <StyledHeader>{ relatedQuestionsTitle }</StyledHeader>;
};

export default RelatedQuestionsHeader;
